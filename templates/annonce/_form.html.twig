
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div style="display: flex; justify-content:center;">
    <button class="btn">{{ button_label|default('Publier') }}</button>
    </div>
{{ form_end(form) }}
